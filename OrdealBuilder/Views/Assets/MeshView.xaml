<UserControl x:Class="OrdealBuilder.Views.MeshView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:OrdealBuilder.Views"
             xmlns:viewmodels="clr-namespace:OrdealBuilder.ViewModels"
             xmlns:views="clr-namespace:OrdealBuilder.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" Background="White">
    <Grid Margin="0,10,0,0">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition Height="30"/>
        </Grid.RowDefinitions>
        <Label Grid.Column="0" Grid.Row="0" Content="Vertices:"/>
        <Label Grid.Column="1" Grid.Row="0" Content="{Binding VerticesNum}"/>
        <Label Grid.Column="0" Grid.Row="1" Content="Indicies:"/>
        <Label Grid.Column="1" Grid.Row="1" Content="{Binding IndiciesNum}"/>
    </Grid>
</UserControl>
